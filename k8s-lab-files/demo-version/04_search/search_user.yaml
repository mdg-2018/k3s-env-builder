apiVersion: mongodb.com/v1
kind: MongoDBUser
metadata:
  name: search-sync-source-user
  namespace: mongodb
spec:
  passwordSecretKeyRef:
  ## TODO: set the reference to the password secret you created
    name: demo-search-sync-source-password
    key: password
  username: "search-sync-source"
  db: "admin"
  mongodbResourceRef:
    name: "demo"
  roles:
  - db: "admin"
    name: "searchCoordinator"