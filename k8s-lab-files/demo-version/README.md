These files are used to deploy a fully automated demo environment with the following components:
- Ops Manager
- A 3 node replica set with a database user created
- A mongot configured to work with the replica set